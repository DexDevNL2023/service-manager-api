spring:
  cloud:
    consul:
      config:
        fail-fast: true
        format: JSON
        profile-separator: '-'
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 100
